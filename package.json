{
  "name": "threadtrail",
  "version": "0.1.0",
  "private": true,
  "description": "ThreadTrail for DeepSeek Harness — compare any two git records of a session workspace (commits plus the uncommitted worktree state) as a syntax-highlighted diff, as a pnpm TypeScript workspace.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
