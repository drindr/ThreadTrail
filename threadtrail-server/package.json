{
  "name": "threadtrail-server",
  "version": "0.1.0",
  "description": "ThreadTrail git-diff service for DeepSeek Harness: lists a session workspace's comparable records (commits + the uncommitted worktree state) and serves the unified diff between any two of them.",
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    ".": "./dist/index.js",
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test \"test/*.test.ts\""
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  }
}
